# Ticket Management API

Simple FastAPI ticket API with SQLite.

## Run

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```

Docs: `http://127.0.0.1:8000/docs`
